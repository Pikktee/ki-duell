{
  "name": "ki-duell-functions",
  "version": "0.1.0",
  "description": "Proxy für KI-Duell: Text- (OpenRouter) und Bild-Generierung (Replicate), Tages-Challenge, Leaderboard.",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "admin": "python3 -m http.server 5055 --directory ../admin"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "private": true
}
